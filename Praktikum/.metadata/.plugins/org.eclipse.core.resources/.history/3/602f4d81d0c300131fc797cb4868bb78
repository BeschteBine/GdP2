import studiplayer.basic.WavParamReader;

public class WavFile extends SampledFile {

	// constructors
	public WavFile() {
		super();
	}

	public WavFile(String s) {
		super(s);
		readAndSetDurationFromFile(this.getPathname());
	}

	// attributes

	public void readAndSetDurationFromFile(String pathname) {
		// liest Werte für Gesamtanzahl der Frames und Frame Rate,
		// der AudioDatei(pathname)mit Methoden aus class WavParamReader
		// und speichert in Attribut
		WavParamReader.readParams(pathname);
		setDuration(WavFile.computeDuration(WavParamReader.getNumberOfFrames(),
				WavParamReader.getFrameRate()));
	}

	public String toString() {
		return "";
	}

	public String[] fields() {
		return null;
	}

	public static long computeDuration(long numberOfFrames, float frameRate) {

		// float mt = Float.parseFloat(numberOfFrames);
		// Gesamtanzahl Frames / Frame Rate = Gesamtspieldauer in microseconds
		return (long) (numberOfFrames / frameRate * 1000000);
	}
}
